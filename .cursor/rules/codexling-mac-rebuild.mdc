---
description: Codexling macOS 改代码后自动打包并重启 dist 应用
globs: app/Codexling/**/*
alwaysApply: false
---

# Codexling 打包重启

在本仓库修改 `app/Codexling` 下的 Swift 源码、资源或 `Package.swift` 并完成相关改动后，**在同一轮对话末尾**（无需用户再提醒）执行：

```bash
cd app/Codexling && ./rebuild_and_run.sh
```

- 使用 `rebuild_and_run.sh`（内部调用 `package_app.sh`，再 `pkill -x Codexling` 并 `open dist/Codexling.app`）。
- 需要完整权限以便结束进程并启动 GUI 应用（`required_permissions: ["all"]`）。
- 若仅文档/注释且未动可执行产物，可跳过；若用户明确要求「只编译不重启」则只跑 `swift build`。
- 打包失败时汇报编译错误，不要假装已重启。
